{% extends 'base.html.twig' %}

{% block body %}
    <h1>home page</h1>

    <p>
        welcome to the home page !!
    </p>
<table>
    <thead>
    <tr>
        <th>Id</th>
        <th>Name</th>
        <th>Photo</th>
        <th>Description</th>
        <th>Ingredients</th>
        <th>PriceRange</th>
        <th>actions</th>
    </tr>
    </thead>
    <tbody>
    {% for port in ports %}
        <tr>
            <td>{{ port.id }}</td>
            <td>{{ port.name }}</td>
            <td>{{ port.photo }}</td>
            <td>{{ port.description }}</td>
            <td>{{ port.ingrediants }}</td>
            <td>{{ port.priceRange }}</td>

        </tr>
    {% else %}
        <tr>
            <td colspan="7">no records found</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{% endblock %}
